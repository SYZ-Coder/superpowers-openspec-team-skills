# OpenCode Bundle: OpenSpec Feature

Install this bundle when you only need a standalone OpenCode skill for OpenSpec change authoring before implementation.

Install target:

```text
.opencode/skills/
```

After installation, invoke:

```text
Use the openspec-feature-workflow skill to produce the OpenSpec change artifacts for this request.
```
