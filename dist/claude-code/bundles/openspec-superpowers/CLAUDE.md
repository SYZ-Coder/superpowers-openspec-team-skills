# Project Workflow

These workflow instructions are explicit opt-in. Do not apply them by default. Only use them when the user explicitly asks for the workflow or invokes its command.

Prefer the `openspec-superpowers-workflow` command when a feature needs clarification, OpenSpec artifacts, implementation planning, TDD, and verification.
